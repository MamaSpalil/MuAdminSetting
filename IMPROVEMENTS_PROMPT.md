# Промт: улучшение функциональности и визуала MuAdmin

Этот документ — постановка задачи и пошаговый план работ по улучшению
программы **MuAdmin** (графический редактор серверных конфигов MU Online).
Документ предназначен для использования как ТЗ для последующих коммитов /
сессий: каждая фаза самодостаточна, имеет критерии готовности и не ломает
обратную совместимость с уже существующими файлами сервера (round-trip
сохраняется — см. `src/MuAdmin.Core/Parsers/*`).

> **Важное замечание об авторских правах.**
> Официальные спрайты монстров, иконки предметов и скриншоты локаций
> MU Online являются собственностью Webzen Inc. Их нельзя «скачать из
> открытого доступа» и закоммитить в этот репозиторий — это нарушение
> копирайта. Поэтому архитектура визуализации построена так:
> программа ищет картинки в локальной папке `assets/` рядом с исполняемым
> файлом (или указанной пользователем). Пользователь кладёт туда свои,
> легально полученные изображения (например, экспортированные из
> собственного клиента). Если файла нет — рисуется аккуратный
> плейсхолдер. Это обеспечивает и легальность, и полноценный визуал
> у конечного пользователя.

---

## 1. Исправление ошибок табуляции и проверка столбцов

Файлы сервера MU — это в основном whitespace-разделённые таблицы с
комментариями (`//`) и маркерами `end`. Сейчас `TabularEditor` показывает
безымянные столбцы `c1..cN`, что затрудняет редактирование и
провоцирует ошибки.

### 1.1 Схемы столбцов
- [ ] Создать `src/MuAdmin.Core/Schemas/TabularSchema.cs` —
      описание схемы файла: имя файла (glob), список колонок
      (`Name`, `Type`, `Description`, `Required`, опц. `EnumValues`).
- [ ] Создать `src/MuAdmin.Core/Schemas/SchemaRegistry.cs` со
      встроенными схемами для основных файлов:
  - `Monsters/Monster.txt`
  - `Monsters/MonsterSetBase*.txt`
  - `Items/Item.txt`, `Items/ItemAddOption.txt`
  - `Quests/Quest.txt`, `Quests/QuestRewardSetting.txt`
  - `Move/MoveReq.txt`
  - `Shops/Shop_*.txt`
  - `EventItemBags/*.txt`
- [ ] Источник истины — официальная документация формата серверов
      (комментарии в шапке каждого `.txt`). Парсить шапку и, если
      встроенной схемы нет, выводить колонки из неё.

### 1.2 Применение схемы в редакторе
- [ ] В `TabularEditor.BuildTable` использовать схему: задавать
      `Caption` колонкам, `ToolTipText` с описанием, `MinimumWidth`,
      `DefaultCellStyle.Format` (числа — выравнивание вправо).
- [ ] Колонки сверх схемы помечать как `Extra` и подсвечивать —
      это типичный признак ошибки табуляции.
- [ ] Колонки до конца схемы, но пустые — подсвечивать жёлтым,
      если поле `Required`.

### 1.3 Валидация
- [ ] Добавить `TabularValidator` (Core): проверяет
  - количество колонок в каждой data-строке против схемы;
  - типы (`int`, `hex`, `float`, `bool`, `enum`);
  - диапазоны (если заданы в схеме);
  - ссылочную целостность (например, `MonsterIndex` в
    `MonsterSetBase` существует в `Monster.txt`).
- [ ] Панель «Проблемы» внизу редактора со списком предупреждений и
      двойным кликом — переход к ячейке.

### 1.4 Визуальное выравнивание
- [ ] Использовать моноширинный шрифт (`Consolas, 9pt`) в `DataGridView`,
      авто-ширину по содержимому (`AutoSizeMode = AllCells`) с
      разумными пределами.
- [ ] Чередующаяся подсветка строк (`AlternatingRowsDefaultCellStyle`).
- [ ] Включить «заморозку» первых N колонок (по схеме — обычно
      `Index` + `Name`).

**Готово, когда:** все встроенные файлы открываются с понятными
заголовками, лишние/пропущенные колонки видны визуально, валидатор
выдаёт ≤ известных проблем на чистых данных, round-trip сохраняется
(тест: открыть → сохранить без правок → `git diff` пуст).

---

## 2. Инфраструктура ассетов (фундамент визуализации)

### 2.1 Папка и формат
- [ ] Соглашение: `assets/` рядом с `MuAdmin.exe` (и переопределение
      через меню «Файл → Папка ассетов…»). Структура:
  ```
  assets/
    monsters/<MonsterIndex>.png        // напр. 0.png, 25.png
    monsters/<MonsterIndex>.jpg
    items/<Section>_<Index>.png        // напр. 14_13.png (Jewel of Bless)
    maps/<MapNumber>.png               // напр. 0.png (Lorencia)
    placeholders/monster.png
    placeholders/item.png
    placeholders/map.png
  ```
- [ ] README в `assets/README.md` — как добавлять свои картинки,
      легальные источники (собственный клиент, `Data/Item/`,
      `Data/World*/`).

### 2.2 `AssetsManager`
- [ ] `src/MuAdmin.Core/Assets/AssetsManager.cs`:
  - кеш `Image` по ключу (LRU, лимит ~256 элементов);
  - методы `GetMonster(int index)`, `GetItem(int section, int index)`,
    `GetMap(int mapNumber)`;
  - возвращает плейсхолдер при отсутствии файла;
  - потокобезопасный чтения (lock на словарь).
- [ ] `src/MuAdmin.Core/Assets/PlaceholderRenderer.cs`:
  генерирует Bitmap с центрированным текстом (например, «M:25»),
  пастельным фоном по хэшу ключа.
- [ ] Внедрить экземпляр `AssetsManager` в `ServerProject` (или
      синглтон `App.Assets`), пробрасывать в редакторы через
      `IFileEditor.Load(path, project)`.

### 2.3 Утилизация
- [ ] Корректный `Dispose` на смене проекта/закрытии формы
      (`Bitmap` и `LRU`-кеш).

**Готово, когда:** есть API `assets.GetMonster(0)` — возвращает либо
картинку из `assets/monsters/0.png`, либо плейсхолдер; покрыто
unit-тестами (создаётся временная папка с тестовым `.png`).

---

## 3. Визуализация монстров

### 3.1 Колонка-картинка в каталоге монстров
- [ ] В `TabularEditor` для `Monsters/Monster.txt` добавить
      `DataGridViewImageColumn` слева — картинка монстра по индексу из
      колонки `Index`. Размер 32×32, `ImageLayout = Zoom`.

### 3.2 Превью в редакторе спотов
- [ ] Для `Monsters/MonsterSetBase*.txt` (см. парсер `TabularFileParser`
      и `MonsterCatalog`) — справа от грида панель `MonsterPreviewPanel`:
  - крупная картинка (256×256) выделенного монстра;
  - имя из `MonsterCatalog`;
  - ключевые статы из `Monster.txt` (HP, Level, AttackMin/Max).
- [ ] При смене выделения строки в гриде — обновлять панель.

### 3.3 Тултип в гриде
- [ ] При наведении на ячейку `MonsterIndex` показывать
      `ToolTip` с миниатюрой и именем (используя
      `DataGridView.CellMouseEnter`/`ShowCellToolTips=false` +
      собственный `ToolTip` с `OwnerDraw`).

**Готово, когда:** в `MonsterSetBase0.txt` при клике на строку
видно картинку и характеристики монстра; при отсутствии файла
картинки — корректный плейсхолдер.

---

## 4. Визуализация наград (вещей) для квестов

### 4.1 Расширить парсер квестов
- [ ] В `QuestSystemFileParser` (файл `Quests/Quest.txt`,
      `QuestRewardSetting.txt`) убедиться, что для каждой награды
      доступны `ItemSection`, `ItemIndex`, `Level`, `Quantity`.
      Если нет — допарсить.

### 4.2 `ItemCatalog` → отображение
- [ ] В `ItemCatalog` добавить `GetDisplayName(section, index)` и
      `GetIcon(section, index)` (через `AssetsManager.GetItem`).

### 4.3 UI в `QuestSystemEditor`
- [ ] Колонка-иконка в списке наград.
- [ ] Боковая панель «Награды квеста»: список карточек
      (иконка 48×48 + название + количество + уровень).
- [ ] Двойной клик по награде — диалог выбора предмета из
      `ItemCatalog` с поиском по имени и фильтром по секции.

**Готово, когда:** для любого квеста с наградами в
`Quests/Quest.txt` отображается читаемый список с иконками; при
изменении `ItemSection`/`ItemIndex` иконка обновляется без
перезагрузки.

---

## 5. Визуализация локаций (карт) для спотов

### 5.1 Каталог карт
- [ ] Использовать `MapNames` (`src/MuAdmin.Core/Catalogs/MapNames.cs`),
      добавить `GetMapNumberByName` если отсутствует.

### 5.2 Превью карты
- [ ] В редакторе `MonsterSetBase*.txt` — над панелью монстра
      показывать `MapPreviewPanel`:
  - картинка карты из `assets/maps/<MapNumber>.png`;
  - имя карты;
  - оверлей точек спавна по координатам (`StartX`, `StartY`,
    `EndX`, `EndY`) — рисовать прямоугольники зон с подсветкой
    выделенной строки.
- [ ] Координаты MU — 0..255 по обеим осям; масштабировать к размеру
      картинки.

### 5.3 Источники карт (легально)
- [ ] В `assets/README.md` указать: пользователь сам экспортирует мини-карты
      из своего клиента (`Data/World*/EncTerrain*.att` + рендер) или
      использует свои собственные изображения. Не коммитить
      официальные ассеты Webzen.

**Готово, когда:** при открытии `MonsterSetBase0.txt` (Lorencia)
видно карту с прямоугольниками зон спавна; при выборе строки в
гриде соответствующий прямоугольник подсвечивается.

---

## 6. Общие визуальные улучшения

- [ ] Тёмная тема (опционально, через `VisualStyleRenderer` +
      перекраска `DataGridView`).
- [ ] Иконки в дереве файлов (`FileCategoryTab`) по типу файла
      (`.txt` таблица, `.cfg` ini, и т.п.).
- [ ] Статус-бар: путь активного файла, кодировка, количество строк,
      количество ошибок валидации.
- [ ] Запоминание последнего открытого проекта и состояния вкладок
      (через `Properties.Settings`).
- [ ] Горячие клавиши: `Ctrl+S`, `Ctrl+Shift+S`, `F5`, `Ctrl+F` (поиск
      по гриду).

---

## 7. Качество кода и инфраструктура

- [ ] Unit-тесты:
  - `TabularFileParser` round-trip для всех файлов из репозитория;
  - `AssetsManager` (плейсхолдер, кеш, отсутствующий файл);
  - `TabularValidator` (типы, ссылки).
- [ ] Проект `tests/MuAdmin.Core.Tests` (xUnit, .NET Framework 4.8 или
      `net48`).
- [ ] CI: `dotnet build` + `dotnet test` в GitHub Actions
      (Windows runner, т.к. WinForms; либо собирать только Core на
      Linux + тесты Core).
- [ ] Соблюдать существующие соглашения: `LangVersion 7.3`,
      ссылки через `<Reference>`, `Modified=true` только при реальных
      правках (round-trip!).

---

## Порядок выполнения (фазы)

1. **Фаза 1 — Фундамент.** Раздел 2 (`AssetsManager` + структура
   `assets/`). Без UI-изменений. Покрыть unit-тестами.
2. **Фаза 2 — Табуляция/столбцы.** Раздел 1 (схемы + применение в
   `TabularEditor` + базовая валидация).
3. **Фаза 3 — Монстры.** Раздел 3.
4. **Фаза 4 — Награды квестов.** Раздел 4.
5. **Фаза 5 — Карты спотов.** Раздел 5.
6. **Фаза 6 — Полировка UI.** Раздел 6.
7. **Фаза 7 — Тесты и CI.** Раздел 7 (часть тестов добавляется по
   ходу фаз 1–5).

Каждая фаза = отдельный коммит/PR. После фазы:
- `dotnet build` зелёный;
- ручная проверка: открыть локальный сервер, убедиться, что
  существующие файлы открываются и сохраняются без diff’а, если
  правок не было.

---

## Критерии «профессионально выполнено»

- Никаких регрессий round-trip (см. memory: `OriginalText`/`Modified`).
- Никаких копирайт-нарушающих ассетов в репозитории.
- Все новые публичные классы/методы имеют XML-doc на русском в стиле
  существующих (`/// <summary>`).
- Соответствие целям проекта: `.NET Framework 4.8`, `LangVersion 7.3`,
  явные `<Reference>` в `.csproj`, без новых тяжёлых зависимостей
  (для картинок достаточно `System.Drawing`).
- Каждое UI-изменение проверено вручную на реальных файлах из
  корня репозитория (`Monsters/`, `Quests/`, `Items/`).
